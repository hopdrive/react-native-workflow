#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run type checking
npm run typecheck

# Run linting
npm run lint

# Run tests with coverage (will fail if thresholds not met)
npm run test:coverage
